from setuptools import setup, find_packages

setup(
    name="python_module_matrix",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # List package dependencies here: e.g.,
        # 'numpy>=1.18.0'
    ]#,
    # Add more package metadata
)
